---
name: ripgrep
author: Andrew Gallant
homepage: https://ripgrep.dev/
project: https://github.com/BurntSushi/ripgrep
language: Rust
alternativeto: grep
description: Extremely fast line-oriented search tool that respects .gitignore rules.
last_updated: 2026-05-03
screenshots:
    - file: screenshot.png
      caption: ripgrep searching a codebase
links:
    - title: "Why I switched to ripgrep from grep"
      url: https://medium.com/towards-data-engineering/why-i-switched-to-ripgrep-from-grep-3a4f3ad6d9ce
      published: 2026-01-08
      fetched: 2026-05-03
    - title: "Fast Searching with ripgrep"
      url: https://mariusschulz.com/blog/fast-searching-with-ripgrep
      published: 2020-03-19
      fetched: 2026-05-03
body: |
    ripgrep is a line-oriented search tool that recursively searches your
    current directory for a regex pattern. By default, `rg` will respect
    your `.gitignore` and automatically skip hidden files/directories and binary
    files. ripgrep has first class support on Windows, macOS and Linux, with
    binary downloads available for every release. ripgrep is similar to other
    popular search tools like [ack](/tool/ack) and [ag](/tool/ag).

    {{screenshot: screenshot.png, ripgrep searching a codebase}}

    ## Basic usage

    ```
    rg pattern
    rg -t py pattern        # Search only Python files.
    rg -l pattern           # List only matching filenames.
    rg --no-ignore pattern  # Ignore .gitignore rules.
    rg -i pattern           # Case-insensitive search.
    ```
