---
name: fd
author: David Peter
homepage:
project: https://github.com/sharkdp/fd
language: Rust
license: MIT
alternativeto: find
description: Simple, fast, and user-friendly alternative to find.
last_updated: 2026-03-15
links:
    - title: A Friendly Alternative to find
      url: https://opensource.com/article/18/6/friendly-alternative-find
screenshots:
    - file: screencast.svg
      caption: fd in action
body: |
    `fd` is a simple, fast and user-friendly alternative to `find`.

    While it does not seek to mirror all of `find`'s powerful functionality, it provides sensible
    (opinionated) defaults for 80% of the use cases.

    - Convenient syntax: `fd PATTERN` instead of `find -iname 'PATTERN'`
    - Colorized terminal output (similar to `ls`)
    - It's fast
    - Smart case: the search is case-insensitive by default. It switches to
      case-sensitive if the pattern contains an uppercase character
    - Ignores hidden directories and files, by default
    - Ignores patterns from your `.gitignore`, by default
    - Regular expressions
    - Unicode-awareness
    - Parallel command execution with a syntax similar to GNU Parallel

    ## Demo

    {{screenshot: screencast.svg, fd in action}}

    fd is from the same author as [bat](/tool/bat) and [diskus](/tool/diskus).
